# Relation extraction over a tiny fixture slice with gold-echo mock LLM.
# Replace llm with a real provider and documents.path with a full
# preprocessed dataset for actual benchmarking.
project:
  name: relation_extraction_fincausal_tiny
  run_id: re_demo_001

task:
  name: relation_extraction
  # relation_types omitted -> taken from each document's gold relations

llm:
  provider: mock
  reply: '{"CAUSE": []}'

documents:
  path: ../../tests/fixtures/relations/fincausal_tiny.jsonl

outputs:
  directory: outputs/re_demo
